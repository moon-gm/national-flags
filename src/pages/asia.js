import GroupPage from '../components/groupPage'

export default function Asia({funcs, nationalData}) {
  return (
    <GroupPage
      group="asia"
      funcs={funcs}
      nationalData={nationalData}
    />
  )
}