import GroupPage from '../components/groupPage'

export default function Africa({funcs, nationalData}) {
  return (
    <GroupPage
      group="africa"
      funcs={funcs}
      nationalData={nationalData}
    />
  )
}
